const state = {
  items: []
}

const getters = {
  getTotalPrice: state => {
    return state.items
  }
}

const mutations = {}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}